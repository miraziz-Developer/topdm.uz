# Merchant CRM

Next.js 14 — sotuvchi paneli: mahsulot, buyurtma, chat, analytics.

## Tuzilma

| Papka | Vazifa |
|-------|--------|
| `app/dashboard/` | CRM bo‘limlari |
| `src/components/` | Panel komponentlari |
| `src/lib/` | API client |

## Ishga tushirish

```bash
docker compose up merchant-crm   # :3003
# yoki
npm ci && npm run dev
```

Konfiguratsiya: root `.env`
