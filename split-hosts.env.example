# 2×4GB split — droplet IP larni shu yerga yozing (gitga commit qilmang)
# cp split-hosts.env.example split-hosts.env

# DigitalOcean → Droplets → har biri → Public / Private IP

WEB_PUBLIC_IP=
WEB_PRIVATE_IP=

CORE_PUBLIC_IP=
CORE_PRIVATE_IP=

# Qaysi server qaysi rol
# WEB = DNS (@, www, api, crm) shu public IP ga
# CORE = faqat ichki API, public IP faqat SSH uchun
